{
  "name": "get-started",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=14.x"
  },
  "scripts": {
    "start": "cp gh-config.js app-config.js && parcel get_started.html --port 3000",
    "build": "cp gh-config.js app-config.js && parcel build get_started.html --dist-dir lib --no-content-hash"
  },
  "devDependencies": {
    "buffer": "^5.7.1",
    "parcel": "^2.8.3",
    "process": "^0.11.10",
    "tslib": "^2.3.1",
    "typescript": "^4.9.5",
    "url": "^0.11.3"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "^0.2.38"
  }
}
